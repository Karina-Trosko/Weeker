import EStyleSheet from 'react-native-extended-stylesheet';

const repeatIconStyle = EStyleSheet.create({
    repeatIconText: {
        position: 'absolute',
        fontWeight: '600',
    },
    container: {
        marginHorizontal: 10,
    },
});

export default repeatIconStyle;
